version: 2.1
jobs:
 build:
   machine: true
   working_directory: ~/grafana/image 
   environment:
     GRAFANA_VERSION: 7.2.2
   steps:
     - checkout
     - run: |
         echo "$DOCKER_PASS" | docker login --username $DOCKER_USER --password-stdin
     - run: docker build --build-arg GRAFANA_VERSION=$GRAFANA_VERSION -t levkov/grafana:$GRAFANA_VERSION .
     - run: docker push levkov/grafana:$GRAFANA_VERSION